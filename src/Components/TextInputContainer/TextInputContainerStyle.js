import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textinputCon: {
    backgroundColor: '#a1a1a133',
    height: 45,
    marginHorizontal: 10,
    marginTop: 10,
    borderRadius: 4,
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10
  }
});